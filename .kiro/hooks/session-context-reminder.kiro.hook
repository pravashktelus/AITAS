{
  "enabled": false,
  "name": "Session Context Reminder",
  "description": "DISABLED: Already injected via steering files. Fires on every prompt causing double agent roundtrip.",
  "version": "1",
  "when": {
    "type": "promptSubmit"
  },
  "then": {
    "type": "askAgent",
    "prompt": "Context reminder - disabled."
  }
}